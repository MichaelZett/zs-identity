pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'zs-identity'

include ':identity-core'
include ':identity-vaadin'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
